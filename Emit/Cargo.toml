[package]
name = "Emit"
version = "0.1.0"
authors = ["marek counts <marek.counts@outlook.com>"]

[lib]
name = "emit_lib"
path = "src/lib.rs"

[[bin]]
name = "emit"
path = "src/bin/main.rs"

[dependencies]
clap = "~2.32"
